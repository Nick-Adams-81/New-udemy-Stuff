package Trees;

public class BST {


    private Node root;

    static class Node {
        int value;
        Node right;
        Node left;
        Node(int value) {
            this.value = value;
        }
    }

    // lookup O(log n)

    // insert O(log n)

    // remove O(log n)
}
